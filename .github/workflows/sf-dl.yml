name: Download and Upload Clonezilla

on:
  repository_dispatch:
  workflow_dispatch:
  #push:
  #  branches:
  #    - main

jobs:
  download-and-upload:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Download Clonezilla ISO
    #clonezilla-live-3.1.3-16-i686.iso
      run: wget -O clonezilla-live.iso "https://master.dl.sourceforge.net/project/clonezilla/clonezilla_live_stable/3.1.3-16/clonezilla-live-3.1.3-16-i686.iso?viasf=1"

    - name: Create download directory
      run: mkdir -p download

    - name: Move ISO to download directory
      run: mv clonezilla-live.iso download/

    - name: Commit and push changes
      run: |
        git config --local user.name "GitHub Actions"
        git config --local user.email "actions@github.com"
        git add download/clonezilla-live.iso
        git commit -m "Add Clonezilla ISO"
        git push
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
